import { ApiProperty } from '@nestjs/swagger';
import { Transform } from 'class-transformer';
import { IsNotEmpty, IsString, MaxLength, MinLength } from 'class-validator';

export class {{pascalCase nameEntity}}sEventsRoomDTO {
  @ApiProperty({
    example: 'typescript',
    description: 'Sala donde se recibirán los mensajes',
    required: true,
  })
  @IsString()
  @IsNotEmpty()
  @MinLength(2)
  @MaxLength(30)
  @Transform(({ value }) => value?.trim())
  room: string;
}

export class {{pascalCase nameEntity}}sEventsMessageDTO extends {{pascalCase nameEntity}}sEventsRoomDTO {
  @ApiProperty({
    example: 'Hello Room',
    description: 'Mensaje',
    required: true,
  })
  @IsString()
  @MinLength(2)
  @MaxLength(30)
  @Transform(({ value }) => value?.trim())
  message: string;
}
