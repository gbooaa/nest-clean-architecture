import { ApiProperty, PartialType } from '@nestjs/swagger';
import { Transform } from 'class-transformer';
import { IsString, MaxLength, MinLength } from 'class-validator';

export class Create{{pascalCase nameEntity}}DTO {
  @ApiProperty({
    example: '{{nameEntity}}',
    description: '{{nameEntity}}',
    required: true,
  })
  @IsString()
  @MinLength(2)
  @MaxLength(30)
  @Transform(({ value }) => value.trim())
  name: string;
}

export class Update{{pascalCase nameEntity}}DTO extends PartialType(Create{{pascalCase nameEntity}}DTO) {}
